{
  "name": "pdf-to-csv-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Reliable PDF-to-CSV pipeline with validation, batch processing, and sample verification.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/main.ts",
    "lint": "echo \"No lint configured yet\"",
    "test": "jest --runInBand",
    "prepare": "npm run build"
  },
  "bin": {
    "pdf-to-csv": "src/main.ts"
  },
  "dependencies": {
    "csv-stringify": "^6.0.0",
    "dotenv": "^16.3.1",
    "pdf-parse": "^1.1.1",
    "p-limit": "^4.0.0",
    "winston": "^3.9.0",
    "commander": "^11.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "@types/node": "^20.13.4",
    "jest": "^29.6.4",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.6.2"
  }
}
